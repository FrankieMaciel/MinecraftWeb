function createChunk(x, smooth, intensity, chunkSize) {

  let Xsmooth = x / smooth;
  let y = parseInt(perlin.get(Xsmooth, Xsmooth) * intensity);

  let max = x + chunkSize;

  for (let xpos = x; x < max; x++) {

    for (let y = y; y < )

  }
}